# 凤凰血月终章未生成排查（2026-09-22）

## 结论

本次血月终章已经触发，刷新流程在实体编号校验处中止。`PhoenixEncounter.Update` 把 `EntityClass.FromString` 返回的负整数判为“实体不存在”；本机游戏程序集显示该方法只对输入名称调用 `GetHashCode()`，并不查询实体注册表，也没有“负数表示找不到”的合同。因此报错文字不能作为缺少XML实体定义的证据。

抛出异常后 `blocked=true`，同一世界会话后续更新直接返回，不再重试。本次失败发生在 `EntityFactory.CreateEntity` 和 `Consume` 之前，没有执行凤凰实体生成，也尚未走到凤凰模型加载。

## 日志证据

输入日志：`D:/xwechat_files/van_config_44cf/temp/RWTemp/2026-09/c2a5ea1f84ce0bff17bbac730efc577c/output_log_client__2026-09-22__11-55-31.txt`。

| 行号 | 时间 | 内容 |
| --- | --- | --- |
| 160～162 | 11:55:48 | 加载 `95-PhoenixBoss/YF.Phoenix.dll`，Mod版本0.1.2 |
| 357 | 11:55:50 | `[PhoenixBoss] T16-T19 blood moon finale loaded.` |
| 5881 | 13:48:53 | 第166天血月开始 |
| 8072 | 14:05:41 | `Blood moon is over!`，附近时间为第167天凌晨4点 |
| 8083 | 14:05:42 | `[PhoenixBoss] Dawn finale pending night=166` |
| 8084～8085 | 14:05:42 | `Missing phoenix entity class`，停用当前会话；堆栈定位 `PhoenixEncounter.Update` |

血月后段刷怪记录显示玩家游戏阶段约320206和333272，均为T17。具体错误日志没有打印凤凰名称或整数ID，因此不能从日志直接给出该整数数值。

## 源码与二进制证据

- `95-PhoenixBoss/Source/PhoenixBoss.cs:51`：实体名称计算后调用 `EntityClass.FromString`，紧接着 `if(id<0)` 抛出上述异常。
- 同文件第57行捕获异常并设置 `blocked=true`；第28行使后续更新返回。
- 本地 `95-PhoenixBoss/YF.Phoenix.dll` 的MVID为 `54aa154f-6405-4978-a78a-59563cd37a5b`，与日志堆栈中的模块标识一致。
- 只读检查该DLL的IL：`IL_053e`调用FromString，`IL_0543`开始比较是否非负，失败走到 `Missing phoenix entity class`。与日志偏移 `0x00543` 对应。
- 本机 `7DaysToDie_Data/Managed/Assembly-CSharp.dll` 中 `EntityClass.FromString(string)` 的IL只有加载字符串、调用 `System.Object.GetHashCode()`、返回。
- 同程序集的 `EntityClass.GetEntityClassName(int)` 用 `EntityClass.list.TryGetValue` 查询注册表，未使用整数符号判断存在性。
- 本地 `95-PhoenixBoss/Config/entityclasses.xml` 包含T16、T17、T18、T19四种凤凰定义。日志来自另一台E盘安装，本地XML存在不能单独证明对方XML一致；但相同DLL的错误判断已被明确定位。

依照这段二进制的执行顺序，只有存在合格存活参与者、没有上一只存活凤凰并找到已加载有效空中位置，才会到达此次失败处。因此本次直接阻断点是编号检查。

## 修复建议（本轮未执行）

1. 用实体注册表是否包含返回的ID及注册名称是否匹配目标名称判断存在性，不能使用 `id<0`，也不能只改成 `id==-1`；该API根本不执行不存在查询。
2. 真正缺定义时日志输出目标名称、ID、档位及注册查询结果；与实体构造失败、模型加载失败分别报告。
3. 补测正、负编号的有效定义和真正不存在的定义；验收完整血月结束触发路径，不能仅手动调用实体工厂。
4. 若需要补刷本次血月，先读取该存档的 `phoenix-bloodmoon.xml` 和当前世界时间。此次异常在Consume之前，但记录有重试截止时间，不能保证仅重启即可补刷，也不应直接删除全份记录。

## 检查范围

最初排查只读取用户日志、现有源码、配置及本机程序集IL，没有改代码或存档；未读取日志来源机器的游戏程序集、实际实体注册表或存档状态。

用户随后要求修复：已完成0.1.3注册表校验、当前游戏程序集编译及22项离线回归，在本机游戏退出后备份并安装；完整包可供E盘日志来源机器更新。详见 `Release-0.1.3.md`。未启动游戏或补刷凤凰，未修改存档，实际血月与双客户端仍待验收。
