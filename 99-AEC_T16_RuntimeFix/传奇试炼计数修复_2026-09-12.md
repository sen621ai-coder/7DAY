# 传奇试炼击杀不计数：1.25.9

日志：`%APPDATA%/7DaysToDie/logs/output_log_client__2026-09-11__22-49-32.txt`。

- 任务 `pzaecchallenget16`，编号 `2100917492`，持有者实体 `1968`。
- 三只 Boss 实体 `100953`、`100954`、`100955` 的生成标记均与本场匹配。
- 00:09:41、00:10:54 主机击杀和 00:11:17 队友击杀后，主机目标日志均为 `eligibleNow=True count=0 complete=False`。

根因已用当前安装的游戏程序集离线复现：试炼目标包含名称属性，因而进入原生 `ObjectiveEntityKill.ParseProperties`。没有配置 `target_tags` 时仍调用 `FastTags.Parse("")`，得到非空的空名称标签。原生击杀回调改走标签分支，跳过正确的实体 ID 匹配，无法加分。

修复在 `LegendaryAdventure.BeforeTrialKill` 中接入 `NormalizeTrialTargetTags`：只清理 T16–T19 试炼指定目标中未显式配置的标签，随后继续执行原有场次归属检查和原生计数。旧任务、克隆目标也在回调时修正。未修改奖励、存档、共享范围或生成规则。

验证：

- 编译成功，0 警告、0 错误。
- `tools/Test-LegendaryTrialKills.ps1`：12 种 Boss 的原生击杀回调在修复前均为 0，修复后均为 1，并请求刷新；错误 Boss、显式标签、非试炼任务、克隆目标和场次归属边界通过。
- `tools/Test-LegendaryAdventure.ps1`：现有传奇行动、共享、网络与配置回归通过。
- 离线测试隔离了 UI 与完成发奖，尚不能代替游戏内自动领奖及双人实测。

已部署 `99-AEC_T16_RuntimeFix/AEC.T16.RuntimeFix.dll`；旧 DLL 备份在 `.local-tests/trial-tag-fix/AEC.T16.RuntimeFix-1.25.8-backup.dll`。

主机及队友均需更新整个 RuntimeFix 文件夹并完全重启游戏。已漏记的击杀不自动补回。三只已死的卡住任务需放弃并使用新凭证开始；旧任务不会额外召唤 Boss。
