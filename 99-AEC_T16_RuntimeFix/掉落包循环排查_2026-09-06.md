# 20:58客户端日志：掉落包找回循环

实际日志：`output_log_client__2026-09-06__20-58-16.txt`。Windows目录元数据暂显示0字节，但用FileShare.ReadWrite打开可读到正在写入的内容；不能把目录显示长度当作空日志。

检查快照显示加载1.25.3，角色以LoadedGame读取成功，ERR=0、EXC=0；没有此前告示牌signRenderer或XML补丁失败。主要刷屏是上次修复产生的`Recovered fallen loot`：实体84638重复1119次，88163/88164/88168各1092次，合计4395次。

原因：`Entity.SetPosition(target, true)`没有同步掉落物根Transform。原生`EntityItem.updateTransform`在isPhysicsMaster分支中，下一帧会再次读取根Transform的位置，加Origin.position后写回Entity.position，因此将已找回的包反复拉回地下。之前测试只检查SetPosition后的即时字段值，没有执行这个下一帧回写路径，漏掉了循环。

修复源码已同步实体世界位置、根Transform的场景位置、itemRB的位置，保留原有速度清零、内容及归属保护。测试增加零Origin及日志中两组实际Origin偏移，先复现旧SetPosition-only的回跳，再运行20次物理主控更新、20次插值更新检查修复不会被覆盖。

安装结果：用户退出后已重新编译成功（0警告/0错误），DLL已替换，版本更新为1.25.4。旧DLL备份为`.local-tests/AEC.T16.RuntimeFix-1.25.3.dll`。独立验证服已确认加载1.25.4。

最终验证：`aecsavecheck`通过115项，失败0。三组Origin偏移均成功复现旧SetPosition-only的下一帧回跳；修复后每组20次原生物理主控更新、20次原生插值更新，共120次连续调用均保持地面位置，根Transform和刚体位置正确，原包及归属保留。四份角色存档样本完整读至文件末尾。测试服无EXC和XML补丁失败，已正常关闭；玩家当前主文件/备份/meta共6个文件的哈希与测试前一致。此验证覆盖导致刷屏的具体原生回写路径，尚未观察用户重新进档后的实际日志。

未终止用户游戏，未修改玩家存档，未删除任何掉落包。原始日志快照仅保存在`.local-tests/client-213-review.log`用于比对。
