# 自动化工厂实施计划

## 目标与当前版本
在七日杀 V3.2 整合包中建立可联机使用的工厂系统：自动采矿、运输分拣、农场和食物生产、装备分解、冶炼锻造、炮塔补弹。使用独立 Mod `97-AutomationWorkshop`，设备统一由自动化工作台制造。

当前正式交接基线为 **0.5.2 / Git 08ce610**。已加入五种可见传送带、11 种机器独立模型，并修复新模型的原生方块交互识别。机器配置界面、跨区块运输、分流器及真实双客户端联机验收仍待完成。

## 接续任务交接（2026-09-16，优先阅读）

### 用户最新要求

用户反馈机器菜单只有锁定/解锁，认为应当有机器配置。已核对：**当前生产机器和箱间输送器没有配置窗口**，这是尚未实现的功能，并非已经完成的配置面板打不开。下一对话应接续实现机器配置入口与实际控制逻辑。

本次用户要求更新任务文档后换新 AI 对话继续；本次只更新文档，不改功能、不提交或发布新版本。

### 必须保留的基线与已撤销工作

- Mod 仓库：`E:/Warcraft III/steam/steamapps/common/7 Days To Die/Mods`，分支 `main`，远端 `https://github.com/sen621ai-coder/7DAY.git`。
- 当前已提交、已推送版本：`08ce6108150dd458d357f88aedbd6fb6e44a13b1`，自动化版本 **0.5.2**。
- 用户先反馈传送带不能拆、不能连续铺设、输出堵塞；随后明确表示更新之前提交的版本后传送带正常，并要求还原这轮修改。
- **已还原全部本轮传送带修改**：碰撞体改造、耐久 500 和拆毁返还传送带、输出箱绕过样品过滤、堵塞提示、对应测试改动，以及 0.5.3 版本号。安装目录 DLL、资源包和配置均已恢复 0.5.2，撤销前没有提交/推送/发布 0.5.3。
- 不要把上述已撤销内容当作待安装修复，不要自动重新应用。用户确认正常的是先前提交版。
- 本地此前 0.5.3 的测试日志/临时产物可能仍存在，不代表当前代码，也不能作为当前版本验收证据。测试进程已停止。
- 用户 GUA2 存档另有独立仓库，路径 `C:/Users/yf/AppData/Roaming/7DaysToDie/Saves/Navezgane/GUA2`；本次未改该存档，勿拿来破坏性测试。

### 当前机器实际上如何工作

- 生产机/分拣机/箱间输送器主要挂原生 `TEFeatureLockable`、`TEFeatureSignable`，部分还有 `TEFeatureAutomationState`。文字牌用于运行状态，不是配方/物流设置。
- `MachineDisplay.cs` 负责把碰撞体绑定到原生方块并在瞄准时显示状态；修复可选取不等于已经有控制面板。
- 机器从相邻输入箱取料，生产/分拣依赖输出箱首格样品。箱间输送器按相邻输出箱 → 输入箱搬运。
- 传送带、输入/输出箱及水箱有原生库存；多数生产机器本体没有库存/原生制作队列界面。
- 当前 0.5.2 传送带向输出箱卸货仍受首格样品类型限制；同所有者、同一 16×16 区块、最多 32 段、通电、库存未打开等限制仍然存在。

### 下一步：机器配置窗口（尚未实现）

建议先覆盖箱间输送器、分拣机及生产机器，采用与游戏一致的原生 XUi 风格。具体控件方案由接续实现确定，以下是已向用户说明的方向，不是已完成承诺：

- [ ] 原生交互菜单增加明确的“配置”入口，锁定/解锁保留权限含义。
- [ ] 启动/暂停；显示运行状态、缺电/缺料/满箱等停机原因和适用设备的加工进度。
- [ ] 搬运器选择输入、输出库存；分拣机配置物品过滤及目标库存。
- [ ] 厨房、冶炼、锻造等显示适用产品/配方并允许选择，实际生产遵守材料、工具、解锁和电力规则。
- [ ] 对没有配方的设备使用对应设置，不显示无法生效的通用按钮。分解机保护规则不能因新界面被取消。
- [ ] 明确未配置时是否沿用旧样品/相邻箱模式，确保已有流水线和旧存档继续运行。
- [ ] 配置在服务端验证和保存，同步给客户端；检查所有权/访问权限、范围、目标存在性，客户端不能直接增加物品或写进度。
- [ ] 新增持久化数据时检查旧存档兼容；不要随意调整现有 CompositeFeatures 序列或破坏原生库存。
- [ ] 测试配置保存/读档、改配置后实际执行、两个玩家并发修改和越权请求、开箱与搬运互斥、加工中切换配方不重复扣料/产出。

### 关键文件和验证入口

- `Source/Logistics.cs`：服务端轮询、箱间搬运、分拣、供电和库存占用检查。
- `Source/Production.cs` 及 `Source` 下相关文件：生产流程；先核对文件与实现，不靠说明猜配方逻辑。
- `Source/MachineDisplay.cs`：原生碰撞体识别和瞄准状态。
- `Source/Conveyors.cs`：已恢复的 0.5.2 传送带逻辑。
- `Config/blocks.xml`、`Config/XUi_InGame/xui.xml` 及 `tools/EscortNPC/generate_automation_*.py`：设备特征、窗口及配置生成。修改生成内容时同步生成源，防止以后覆盖。
- 构建：`pwsh -NoProfile -File tools/Build-AutomationWorkshop.ps1`。默认会覆盖安装目录 DLL；游戏运行时先编译到暂存输出，不强行替换占用文件。
- 配置检查：`E:/soft/blender/4.5/python/bin/python.exe tools/Test-AutomationWorkshop.py`。
- 库存测试：`pwsh -NoProfile -File tools/Test-AutomationInventory.ps1`，0.5.2 基线为 1953 项。
- 原生无界面 QA：`tools/Build-AutomationGameQA.ps1`，测试源码位于 `tools/EscortNPC/*GameQA.cs`。隔离环境 `E:/soft/7DTD-Modding/AutomationGameQA`，测试世界名 `AutomationQA_Isolated`，仅使用显式测试参数和存档名保护。
- 已完成的 0.5.2 原生交互回归：16 种模型、四方向 Voxel 射线、所属方块 ID、原生提示及启用的编辑/库存命令。此测试**没有证明配置界面已存在**。
- 原生无界面测试及网络序列化测试不等于图形客户端按键操作或真实双客户端联机测试；报告时分别说明。此前电脑锁屏，不能冒充已经完成界面截图验收。

## 阶段一：制造入口（已实现，待游戏内验收）
- [x] 原生 Workstation 方块、独立配方范围、原生队列与输出槽。
- [x] 在普通工作台制作自动化工作台，配方明确且无额外技能门槛。
- [x] 自动化机架、电机、控制器、水泵、切削和输送组件共六类零件。
- [x] 中文名称与说明、独立原生窗口组，复用原版模型。
- [ ] 两台客户端验证摆放、交互、排队制造、取出产物和重启后队列。

## 阶段二：物流与机器运行基础（基础版本已实现，扩展设置和实机验收待完成）
- [x] 输入/输出箱、分拣机、10W供电接口及制造配方。
- [x] 服务端转移、样品过滤、距离优先、所有权和容器编辑锁检查。
- [x] 同区块保存、原生物品数据复制、1953项独立库存与生产回归。
- [ ] 库存目标、分类规则及手动优先级界面。
- [ ] 双客户端操作、通电、读档和强制终止恢复的实机验收。
- 输入箱、输出箱、控制器、分拣机；第一版相邻容器或短距离连接。
- 服务端统一执行物品转移；客户端只提交配置操作。验证拥有者和队伍权限。
- 按物品、分类、目标库存和优先级过滤；满仓、缺料、断电明确停机。
- 不覆盖箱内物品，不把耐久、品质、模组和自定义数据丢掉。
- 为重复请求、双人同时取物、拆机、掉线、存档恢复建立回归测试。
- 首版区域卸载时暂停，不提供离线补产；限制每次更新转移数量。
- 验收：输入 100 个材料，输出、在途与剩余合计严格等于 100；重连和队列满不会重复搬运。

## 阶段三：采矿、农业、食品
- 采矿机：先验证矿点并定时消耗能量与钻头产出；真实挖掘和地形变化作为后续独立功能，不默认破坏地形。
- 农场控制器：扫描限定范围耕地，使用真实种子，成熟后收割，留种补种。
- 水泵与水箱：供水、储量和不足提示；灌溉增益按实际作物机制适配。
- 自动厨房：固定食谱、库存目标、取料/烹饪/输出；复用整合包食物配方。
- 验收：不隔墙越权收邻居作物、不收未成熟作物；产出与输入、留种数量可核对。

## 阶段四：装备分解与锻造
- 战利品输入 → 过滤 → 可选拆模组 → 分解 → 材料分类。
- 默认保护传奇装备和带模组装备；质量、耐久、装备类型、白名单独立配置。
- 先验证整合包已有回收台和配方，不能靠物品名称猜材料产出。
- 矿石输入 → 冶炼 → 金属库存 → 锻造配方 → 成品输出。
- 复用实际熔炼倍率、燃料与配方，不把同一份材料同时记入炉内和箱子。
- 验收：保护规则可靠、拆卸保留模组数据、掉线不重复加工、输入不足不产出。

## 阶段五：可见运输和防御整合
- [x] 直线、左右转弯、上下坡传送带；服务器逐段处理库存，客户端显示货箱。后端从箱子装货、末端向箱子卸货。
- [ ] 跨区块运输、分流器与独立装卸口。
- 炮塔沿用原生敌我识别和射击；供弹器识别正确弹种，遵循容器与炮塔权限。
- [x] 11 种机器独立静态模型与瞄准状态提示；保留旧方块 ID 和原生存档特征。
- [ ] 机器运行动画、运行声效和专用控制面板。
- 验收：双客户端观察一致、血月大规模设备压力测试、路径断开和拆机后物品守恒。

## 默认设计
- 自动化工作台本身是原生装配台；自动机器的供电和运行由第二阶段实现。
- 核心流程先实现，再增加动画；不创建大量真实掉落物模拟传送带。
- 初版不跨未加载区运行，不补发离线产物。
- 新机器逐台开放制造配方，通过运行验收才作为可用设备发布。
- 不改既有存档地形、不替换原工作台、不更改其他 Mod 的生产配方。
