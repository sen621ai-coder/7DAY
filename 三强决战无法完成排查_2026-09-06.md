# 三强决战无法完成：调查记录

调查对象：当前安装 1.25.4，游戏 V3.2.0 b10。此次为诊断，未修改 DLL、任务 XML 或用户存档。

## 实际证据

- 最新客户端日志 `output_log_client__2026-09-06__21-52-26.txt`：T16 三强实体 88805、88806、88807 分别在 23:21:11、23:23:32、23:24:05 死亡。第一只击杀者 1968，后两只击杀者 1970。
- 按本机原生 `QuestJournal.Read`、`Quest.Read`、`BaseObjective.Read` 格式，只读检查 GUA2 两份当前角色文件中 `pzaecchallenget16` 的记录：均为 Active、phase 1、三个目标值均为 0，且已有 `PZAECAdventureSpawn_v1=PZAECTrialT16`。两个任务码不同。其中一个具有服务器批准标记。
- 这说明不能只解释成队友击杀的两只漏记；目前尚未从尸体或日志取得这三只怪死亡时的 `spawnByName`，不能宣称已逐一证明本场死亡被哪个条件拦截。

## 已复现的缺陷

`LegendaryAdventure.BeforeTrialKill` 要求死亡实体的 `spawnByName` 精确匹配本次任务码。这个字段由游戏事件 ExtraData 带入实体，网络初次生成包能够保留它。但游戏磁盘存档格式不保存此字段：

- 原生 `EntityCreationData.write/read(..., true)` 保留该标记。
- 原生 `EntityCreationData.write/read(..., false)` 不保存该标记。
- 原生 `Chunk.write` 的文件保存分支实际调用上述 false 路径，`Chunk.read` 也传入相应文件模式。
- 原生 `Entity.Write/Read` 与 `EntityAlive.Write/Read` 中没有补存这个字段的逻辑。

执行 `.local-tests/Probe-TrialSave.ps1`，使用本机游戏 DLL 的真实读写方法：

```
network=True length=166 consumed=166 tagRetained=True killAccepted=True
network=False length=100 consumed=100 tagRetained=False killAccepted=False
```

因此试炼怪保存、卸载并从磁盘重载后，会失去任务归属标记；即使任务本人击杀，也会被现有计数前缀过滤。这是单机和联机都存在的确定缺陷，与本次所有目标仍为 0 的表现相符；本场具体是否经历此路径尚需运行状态证据。

## 联机条件

三强任务 XML 设置 `shareable=false`，但这不等于原生系统完全不共享队友击杀。已核对 `GameManager.SharedKillServer/SharedKillClient` 和对应网络包：同队且在共享击杀距离内会发出 `QuestEventManager.EntityKilled`。当前世界共享距离为 100 米。两个玩家分别接的试炼具有不同任务码，不应互相借用另一场试炼的 Boss 计数。

## 后续修复边界

需要把试炼归属保存在原生可持久化的数据中，覆盖生成、网络同步和磁盘重载；保留每场任务隔离。验证应包含磁盘读写，而不能仅测试初始网络包。

已经死亡且历史归属丢失的怪，不能只根据同阶同类型就自动认领或直接把所有活动任务标记完成；需要单独处理卡住任务，避免错发另一场试炼奖励。
